inicio

a := 5;
b         :=         14          ;
otroId := (a - b - b + (3 * b));

escribir(
	a,
	b,
	otroId,
	a-b-b+3*b
);

cuenta := ((1)) + ((2+2*4)) + 2+1*10;

escribir(
	cuenta,
	( ((1)) + ((2+2*4)) + 2+1*10 )
);

fin